name: Build Docker Images for Changed Services
on:
  push:
    branches:
      - main
    paths:
      - 'pitstop-webapp/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build pitstop-webapp Docker image
      run: cd pitstop-webapp && docker build -t pitstop-webapp:latest .
    - name: Login to Docker Hub
      env:
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin

    - name: Push Docker image to Docker Hub
      run: docker tag pitstop-webapp:latest simohassane/pitstop-webapp:latest && docker push simohassane/pitstop-webapp:latest
      
